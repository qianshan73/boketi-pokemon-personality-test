# BokeTI

一个静态网页版宝可梦性格测试。用户完成 15 道 MBTI 风格选择题后，会得到最匹配的宝可梦、性格代码、关键词和简短说明。

## 使用

直接用浏览器打开 `index.html` 即可运行。

## 文件

- `index.html`：页面结构
- `styles.css`：响应式视觉样式
- `script.js`：题目、计分逻辑和结果匹配

图片通过 `assets.pokemon.com` 的 Pokédex artwork 链接加载。
